import React from 'react'
import ReactDOM from 'react-dom/client'
import Company from './components/EmployeeApp'
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Company />
  </React.StrictMode>,
)
